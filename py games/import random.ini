import random


letters=list("abcdefjhigklmnopqrstuwwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ1234567890!&%@^.*")
password1= int(input("Введите пароль: "))
def generate_password(lenght):
    password=""
    for i in range(lenght):
        password += random.choice(letters)
        
    return password
lenght=int(input("Введите длину вашего пароля: "))
print("Сгенерировынный пароль:", generate_password(lenght))

def check_password_strenght(password):
    if len(password) or len(password1) < 8 or password.isalpha():
        print("Ваш пароль слабый, рекомендуем вам сделать его надежнее") 
    elif len(password) or len(password1) >= 8 and password.isalpha():
        print("Ваш пароль средний, рекомендуем сделать его надежнее")
    else:
        len(password) or (password1) >= 12
        print("Отлично у вас надежный пароль")
    return password
password= generate_password(lenght)
print(check_password_strenght(password or password1))


def main():
    choise= int(input("выберите что хотите сделать: \n Напишите 1 чтобы сгенерировать пороль \n Напишите 2 чтобы проверить наденость своего пароля"))
    if choise ==  1:
        print(generate_password(lenght))
    elif choise  == 2:
        print(check_password_strenght(password1))
    return main
print(main)